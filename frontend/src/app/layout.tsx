import './globals.css';
export const metadata={title:'GST Pro — Invoice & Tax Compliance',description:'GST invoicing and compliance platform'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
